//function that multiplies all elements in array by 2 and prints the new array
function multiplyArrayNumbers(arr){
    for (let i = 0; i < arr.length; i++){
        arr[i] = arr[i] * 2 //arr[i]*=2
    }
}
